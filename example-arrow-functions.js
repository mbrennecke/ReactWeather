// var names = ['Matt', 'Alvet', 'Sylvia'];


// var person = {
	// name: 'Matt',
	// greet: function () {
		// names.forEach((name) => {
			// console.log(this.name + ' says hi to ' + name)
		// });
	// }
// };

// person.greet();

function add (a, b) {
	return a + b;
}

addStatement(a, b) => {
	console.log(a+b)
}

console.log(add(3, 4));
console.log(add(9, 9));

addStatement(1, 3);